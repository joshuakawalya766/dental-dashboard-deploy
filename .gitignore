# The real token + generated env — NEVER commit (this is a public repo)
ghcr-token.txt
.ghcr-token
.env
# runtime data created on the clinic machine
data/
images/
exports/
# os/editor
.DS_Store
Thumbs.db
*.log

# any stray copy/backup of the key (editors leave ~ and .bak files behind)
ghcr-token*
.ghcr-token*
*~
!ghcr-token.example   # the TEMPLATE must stay tracked
